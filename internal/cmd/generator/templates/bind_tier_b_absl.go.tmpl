//go:build googlesql_tier_b_absl && (linux || darwin)

package {{ .Package }}

// Link Bazel-built libabsl_cgo.a instead of compiling bind.cc for this package.
// Build the archive: `make prebuilt-libs-absl` from repo root.
// Tag policy: mutually exclusive with googlesql_tier_b in the same binary (see docs/prebuilt-absl-overlap.md).
// When editing bind_tier_b_absl.go or the default protobuf bind files, cross-check tag wiring
// against that doc.

/*
#cgo CXXFLAGS: -std=c++20
#cgo CXXFLAGS: -I${SRCDIR}/{{ .IncludeRel }}
#cgo CXXFLAGS: -I${SRCDIR}/{{ .IncludeRel }}protobuf
#cgo CXXFLAGS: -I${SRCDIR}/{{ .IncludeRel }}utf8_range
#cgo linux LDFLAGS: -L${SRCDIR}/{{ .LibRel }} -labsl_cgo -lz -lstdc++ -ldl -lpthread
#cgo darwin LDFLAGS: -L${SRCDIR}/{{ .LibRel }} -labsl_cgo -lz -lc++

void __go_googlesql_tier_b_absl_{{ .AnchorSuffix }}_anchor(void) {}
*/
import "C"
