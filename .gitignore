# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
*.proto
# Test binary, built with `go test -c`
*.test
*.py
# Cursor agent shell hook (repo-owned; permissive default — see file docstring)
!.cursor/hooks/before-shell-execution.py
# Repo maintenance helper (BUILD/orphan audit)
!scripts/ccall-build-orphan-check.py
*.template
*.textproto
*.a
*.csv
*.y
*.l
*.input_file
*.bzl
full_export.inc

# Ephemeral tree for scripts/gen-protobuf-vendor-patches.sh (upstream + amalgamation)
.genpatch-baseline/

# CGO ephemeral output (not checked in)
internal/ccall/go-googlesql/_obj/

# Updater Docker export (Bazel external + execroot; large)
internal/cmd/updater/cache/

# Cloned google/googlesql workspace (see internal/cmd/updater/googlesql.ref, scripts/ensure-googlesql-workspace.sh)
internal/cmd/updater/googlesql/

# Local Python deps / resolved_ast codegen output (not checked in)
internal/gen/
__pycache__/

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Local direnv overrides (optional; see .envrc)
.envrc.local

# Dependency directories (remove the comment below to include it)
# vendor/

*.txt
!internal/ccall/go-googlesql-unified/default_bazel_targets.txt
*.cmake
